{
  "name": "zahnerflow-monorepo",
  "version": "1.0.2",
  "description": "ZahnerFlow 电化学工作流管理系统",
  "private": true,
  "scripts": {
    "version:sync": "node scripts/sync-version.mjs",
    "version:check": "node scripts/check-version.mjs",
    "release:check": "node scripts/check-release-policy.mjs",
    "postinstall": "pnpm --filter @zahnerflow/types build",
    "dev": "concurrently \"pnpm --filter zahnerflow-flowgram dev\" \"uv run python apps/python_backend/main.py\"",
    "desktop:dev": "concurrently \"pnpm --filter zahnerflow-flowgram dev\" \"pnpm --filter zahnerflow-desktop dev\"",
    "desktop:build": "pnpm version:check && pnpm --filter zahnerflow-desktop pack",
    "desktop:dist": "pnpm --filter zahnerflow-desktop dist",
    "desktop:dist:mac": "pnpm --filter zahnerflow-desktop dist:mac",
    "desktop:dist:win": "pnpm --filter zahnerflow-desktop dist:win",
    "backend:dist": "uv run pyinstaller apps/python_backend/zahnerflow-backend.spec --noconfirm --distpath apps/python_backend/dist --workpath .codex-run/pyinstaller-build",
    "build": "pnpm version:check && pnpm --filter @zahnerflow/types build && pnpm --filter zahnerflow-flowgram build",
    "test": "vitest",
    "test:run": "vitest run",
    "test:watch": "vitest watch",
    "test:ui": "vitest --ui",
    "test:coverage": "vitest run --coverage",
    "test:frontend": "vitest run test/code/frontend",
    "test:backend": "vitest run test/code/backend",
    "test:integration": "vitest run test/code/integration",
    "test:e2e": "vitest run test/code/e2e",
    "test:api": "vitest run test/code/integration/api-integration.test.ts",
    "test:websocket": "vitest run test/code/integration/websocket.test.ts",
    "test:report": "node test/scripts/generate-report.js",
    "test:clean": "node test/scripts/clean-results.js",
    "test:validate": "node test/scripts/validate-tests.js",
    "test:performance": "node test/scripts/performance-test.js",
    "test:parallel": "vitest run --reporter=verbose --maxConcurrency=4",
    "test:debug": "vitest --inspect-brk",
    "test:changed": "vitest run --changed",
    "test:fail-fast": "vitest run --fail-fast",
    "test:update-snapshots": "vitest run --update-snapshots",
    "lint": "pnpm --recursive lint",
    "type-check": "pnpm --recursive type-check",
    "clean": "pnpm --recursive clean",
    "setup": "node scripts/setup.js",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^0.34.0",
    "@vitest/ui": "^0.34.0",
    "concurrently": "^8.2.0",
    "prettier": "^3.0.0",
    "typescript": "^5.9.2",
    "vitest": "^0.34.0"
  },
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  },
  "keywords": [
    "electrochemistry",
    "workflow",
    "zahner",
    "gamry",
    "lab-automation"
  ],
  "author": "ZahnerFlow Team",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/your-org/zahnerflow.git"
  }
}
