# ZahnerFlow 设计规范

状态：当前规则。归属：前端样式维护者。来源：`apps/frontend/src/styles/`、`scripts/check-design.mjs` 及下表组件。来源复核：2026-09-20；本次不代表重新视觉验收。

本规范保留现有主工作台布局、组件尺寸和排版。暗色延续玻璃界面；亮色采用专业冷灰白表面，以明确而克制的明度差、柔和边界和轻阴影区分工具栏、侧栏、画布与悬浮卡片。节点库默认使用更亮的独立卡片表面，悬停加强背景与边界，按下和键盘焦点使用蓝色，禁用不提升阴影。

2026-09-20 亮色模式实施范围：在 `codex/light-ui` 分支增加独立亮色配色，复用 `appStore.theme` 和现有持久化，增加顶栏主题切换并修复启动恢复；覆盖主工作台、设备与设置浮层、画布及图表。基线为 `baseline/light-ui-20260920`。当前状态：实现已完成，1600×1000 与 1280×800 浏览器检查通过；版本检查、完整构建、lint 和主题切换/刷新恢复检查通过。浏览器以临时数据目录核对工作台、节点属性、用户设置、管式炉和流量计；未连接真机，未验证 Electron 壳。默认亮色，顶栏月亮/太阳按钮切换；刷新恢复既有 `app-storage` 中的选择。中性前景与凹面通道、主题文字及设备图标统一在 `_tokens.scss` 定义；阴影和遮罩保持独立，不使用整体反色。亮色视觉由 `_light-theme.scss` 在组件层统一适配，不覆盖几何与业务逻辑。

| 维度 | 固定规则 | 实现入口 |
| --- | --- | --- |
| 色彩 | 蓝色表示主要操作；绿/橙/红分别表示成功、警告和失败。设备与测量类型使用有业务名称的专用色 | `_tokens.scss` |
| 文本 | Oxanium 数字/拉丁文字、Noto Sans SC 中文；主文、次文、弱化文字依次使用 `--text-primary/secondary/muted` | `_tokens.scss`、`_base.scss` |
| 间距 | 常规排版使用 `--space-*`；流式控件大小使用 `--size-*`；不得为视觉接近的控件另建一组尺寸 | `_tokens.scss` |
| 面板 | 暗色使用玻璃容器；亮色使用 `--surface-*` 定义工具栏、侧栏、画布、卡片与输入表面，控件保持中性灰表面，外层背景采用浅色渐变粒子波浪 | `_placeholders.scss`、`_light-theme.scss` |
| 按钮 | `.btn` 提供完整默认外观，尺寸和语义通过 `btn--sm/md`、`btn--primary/secondary/...` 组合 | `_buttons.scss` |
| 节点图标 | 一个 SVG 组件、一份几何样式、一份语义配色，库/画布/弹窗共用 | `NodeIconSvg.tsx`、`_node-icons.scss` |
| 弹窗 | 统一 `ModalLayer`，标题/内容/操作区分明；使用画布边界与桌面 chrome 变量定位 | `OverlayLayer.tsx` |
| 交互 | 可见焦点、键盘可达、禁用原因、等待和失败反馈；只有最顶层浮层响应 Escape | 共享浮层与具体组件 |
| 长列表 | 容器滚动，普通行不逐行加 backdrop-filter；展开计划每页最多 100 项 | `_scroll-rendering.scss`、`_unroll.scss` |

核心令牌只在 `_tokens.scss` 定义，响应式布局覆盖集中在 `_base.scss`。业务模块可以定义自身布局参数，但不能重定义核心令牌。静态颜色、间距、圆角等写在 SCSS；运行计算得到的图像尺寸、图表坐标、拖动偏移可以使用内联 style。

Sass 入口保留 `base → layout → components` 的 CSS 层顺序，通过 `meta.load-css` 加载模块。每个使用 mixin 或 placeholder 的模块显式 `@use './placeholders' as *`。不得重新引入旧 `@import` 或跨模块隐式依赖。

`pnpm design:check` 已纳入根 lint：禁止旧导入、检查核心令牌重定义，并对展开窗口、头像裁剪和共享节点图标执行无原始颜色检查。现存图表曲线、硬件状态和装饰渐变仍有业务专色；检查没有宣称全仓库所有颜色字面量都已消除。

变更验证应同时检查主工作台、已有设置弹窗、被修改的弹窗及紧凑窗口。结构检查和构建不能替代截图审阅，浏览器检查也不能证明 Electron 桌面窗口或真机设备行为已验证。

2026-09-20 亮色视觉修订（已实现）：按用户要求改为专业中性冷灰白主题，保持布局、尺寸及功能逻辑。背景、工具栏、侧栏、画布与悬浮卡片采用不同明度；取消亮色极光与粒子装饰、普遍蓝色描边及大面积彩色渐变；选中和设备连接使用克制的蓝色，节点类型及特殊状态保留语义色。已视觉核对工作台、设置、管式炉和流量计。修改前后两档窗口各 9 类组件的坐标与尺寸一致；亮暗切换和刷新恢复通过。完整构建、类型检查、设计规范检查与 lint 通过；临时验证不连接真机。

灰底控件修订（已实现）：以用户截图为准恢复中性灰背景、侧栏、画布及工具条，不再增加全页冷蓝色或渐变。节点卡片恢复柔和边框、轻阴影和悬停、按下、键盘焦点、禁用状态；节点类型与设备图标增强局部语义色。保持布局、尺寸和功能逻辑。浏览器交互与主题刷新恢复通过，页面异常为空；截图及临时验证脚本位于已忽略的 .codex-run/gray-*。
本次版本检查、完整构建与 lint 通过；两档窗口 18 项几何一致，中性背景表面与原灰色版本一致。

亮色背景微调（已完成）：仅将应用最外层底色由 #e6e8eb 提亮为 #f3f4f6 中性灰白，保留面板、画布、卡片与状态样式。版本检查、完整构建与设计规范检查通过。

粒子波浪背景（已实现）：按最新要求，亮色恢复浅色渐变背景并绘制有序粒子波浪；面板与控件维持当前样式，复用背景暂停和可见性生命周期。

当前背景以本次粒子波浪要求为准，替代前述静态灰底限制；系统减少动态效果时保留静态波面。动画、缩放及减少动态效果检查通过，无页面运行异常。
版本检查、完整构建、lint 和类型检查通过；浏览器截图已核对。

亮色玻璃面板修订（已实现）：用户澄清为亮色上下工具栏和左右面板缺少玻璃效果；恢复半透明底色、背景模糊、柔和高光边缘和轻阴影，保留画布、卡片、布局和深色主题。

四个外框面板已核对半透明、16px 背景模糊及轻阴影；截图已审阅，深色面板原透明底色不变。
本次版本检查、完整构建与 lint 通过。

彩色图标试版（已实现）：仅亮色模式的工作站、用户、主题切换及画布常用操作增加局部语义色，保留 SVG 几何、按钮表面与布局；禁用图标继续弱化。

彩色图标截图 .codex-run/color-icons.png 已审阅；完整构建、版本检查与 lint 通过，四周玻璃面板及深色隔离检查通过。

清新亮色配色（已完成）：统一提高亮色图标和强调色的明度，渐变调整为浅青、淡丁香和桃白；保持深灰文字、玻璃效果及控件层级。

本次版本检查、完整构建、lint 通过；玻璃面板和深色隔离验证通过，截图 .codex-run/fresh-colors.png 已审阅。

莫兰迪多彩配色（已完成）：降低亮色强调色饱和度但保留辨识度；节点与工具图标扩展雾蓝、鼠尾草、青绿、豆沙、灰紫、陶土和赭金色相，保留玻璃与浅色背景。

本次版本检查、完整构建和 lint 通过；玻璃面板与深色隔离检查通过，截图 .codex-run/morandi.png 已审阅。

亮色辨识度修订（已实现）：根据用户截图纠正整体加深方向，保留明度并适当提高图标色彩浓度；底栏就绪灯独立使用清亮绿色并移除深色光晕。保持多色相和玻璃层级。
本次构建、版本检查和 lint 通过；浏览器截图已核对，亮色玻璃和深色隔离检查通过。未进行低色域实体屏幕验证。

参考图背景修订（已实现）：参考用户提供的细丝波面、颗粒疏密和波浪边缘，将亮色规则点阵改为分层流线与颗粒消散；继续使用实时画布和现有暂停机制，保留玻璃及控件布局。

浏览器动画变化、减少动态效果暂停、缩放、玻璃面板与深色隔离检查通过；截图 .codex-run/organic-wave.png 已审阅。
画布透明修订（已实现）：中央画布由不透明底色改为半透明浅色底，降低自身模糊以透出背景流线，保留淡网格和独立节点卡片。
最终构建、版本检查、类型检查与 lint 通过，半透明画布截图已审阅。
