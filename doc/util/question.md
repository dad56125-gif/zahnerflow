目标任务：文档整理
具体内容：整理 doc 文件夹下的所有文档，包括子文件夹里的文档。（排除Zahner、util、database文件夹，project-structure文件）。
    1.首先阅读**所有**文件名，根据文件名整理出这些文档的负责范围与作用。（例如：node-节点、execution-执行、bus-事件总线、state-状态机等）
    2.根据负责范围，生成相应的模块文件夹。（规则：Aaaa首字母大写）
    3.将对应的文档转移到相应的模块文件夹内。
    4.**分别**阅读每一个模块文件夹内的所有文档（规则：每个模块文件夹为1个单独的Todo），为[模块名称]模块生成一份核心设计文档，要求内容高度概括，无代码实现。文档需清晰阐述其设计原则、对外接口（API）和主要功能列表，并必须补充说明该模块的核心数据模型、与其他模块的依赖关系，以及一个典型的端到端工作流程（Workflow）。
    5.将所有未排除、已总结整理过的原始文档，整体存入 @archive\doc\[今天的日期] 文件夹中，保留最原始的文件结构，即原先在子文件夹内的文档仍然要在子文件夹内。

    前端：实现插入节点的显示（菱形）、工作流、节点参数的调用，节点保存按钮。
    撤销，重做按钮（删除） 删除按钮 节点右上角
    后端：条件节点的实现 数据库创建的时间
    API：温控api、流量api