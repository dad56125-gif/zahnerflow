# 整体优化记录

开始日期：2026-09-08。原应用版本：1.0.11。

## 回溯基线

- 原提交：`cbd8f58`。
- 基线标签：`baseline/optimization-20260908-v1.0.11`。
- 工作分支：`codex/project-optimization-20260908`。
- 基线工作区干净；原始代码、样式和数据定义均保存在标签中。

## 工作范围与完成条件

| 阶段 | 范围 | 完成条件 |
| --- | --- | --- |
| 一 | 组件职责、历史残留、构建入口 | 按职责拆分复杂组件，移除无调用残留，类型与 lint 通过 |
| 二 | 数据库定义与数据命名 | 数据库结构有单一入口和迁移版本，接口映射集中，既有数据迁移与记录读取通过 |
| 三 | SCSS 规范与展开节点窗口 | 固定核心令牌和复用规则，重做窗口的导航、筛选、选择和运行反馈，完成浏览器检查 |
| 四 | CLI 与 Agent 接入 | 结构化能力发现、命令、JSON 输出及状态观察复用同一运行时，App 可显示外部发起执行 |
| 五 | 集成验证与交付 | 隔离运行、模拟执行、契约/类型/构建、文档及 Git 状态检查通过 |

每个可独立发布阶段判断版本，先同步 `VERSION` 与 `CHANGELOG.md`，完成验证后创建中文提交。测试源码与夹具只放在仓库外或已忽略的 `.codex-run/` 中。

## 执行记录

- 已建立 Git 标签和工作分支。后续章节记录实际变更、验证结果与回溯节点。

### 第一阶段：1.0.12

- 应用根组件移出启动校验、窗口状态和模拟订阅；节点分组与工作站展示由输入派生。
- 删除未使用的设备尺寸 props、面板状态、419 行旧展开器及指向缺失文件的脚本。
- 用户切换按请求代次接收配置，初始化的慢列表响应不能覆盖用户后续选择。
- 各构建发布入口加入版本前置检查，修复版本同步无法重复运行的问题。
- 验证：`pnpm type-check`、`pnpm lint`、`pnpm build`；忽略目录中的用户选择回归测试。构建尚有 Sass 弃用和包体积提示，样式阶段继续处理。

### 第二阶段：2.0.0

- 第一阶段提交：`7e48c2e`。
- 结构定义集中到 `database_schema.py`，迁移版本独立为 1。首次迁移事务化并在补列后建索引；保留原始实验表和数据。
- 用户列表返回真实档案；设置默认值与类型来自共享模型，设置/执行/通知共用读写服务。
- 报告升级到 3.0，由 `report_service.py` 处理 SQL 与旧记录命名；前端直接使用生成契约。操作者归属每次 execution，避免复用工作流导致归属错误。
- 删除两份没有消费者的旧设置表单。
- 因用户列表和报告接口包含不兼容清理，应用采用 MAJOR 升级 1.0.12 → 2.0.0。旧应用消费新接口需同步更新；数据库记录不做破坏性转换。
- 验证：隔离数据断言覆盖迁移、回滚、真实用户、设置合并、历史报告、文件去重；由 Python 输出真实报告夹具验证前端消费。

- 共享类型包改为 ESM，解决生成默认值常量在真实 Vite 打包中无法导入的问题；API 协议独立升级到 4.0.0。
- `pnpm build` 通过；隔离 HTTP 检查确认健康信息、schema 版本、真实用户档案与规范设置响应。命名与迁移规则见 `doc/data-contracts.md`。
