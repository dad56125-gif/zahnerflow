# 整体优化记录

开始日期：2026-09-08。原应用版本：1.0.11。

## 回溯基线

- 原提交：`cbd8f58`。
- 基线标签：`baseline/optimization-20260908-v1.0.11`。
- 工作分支：`codex/project-optimization-20260908`。
- 基线工作区干净；原始代码、样式和数据定义均保存在标签中。

## 工作范围与完成条件

| 阶段 | 范围 | 完成条件 |
| --- | --- | --- |
| 一 | 组件职责、历史残留、构建入口 | 按职责拆分复杂组件，移除无调用残留，类型与 lint 通过 |
| 二 | 数据库定义与数据命名 | 数据库结构有单一入口和迁移版本，接口映射集中，既有数据迁移与记录读取通过 |
| 三 | SCSS 规范与展开节点窗口 | 固定核心令牌和复用规则，重做窗口的导航、筛选、选择和运行反馈，完成浏览器检查 |
| 四 | CLI 与 Agent 接入 | 结构化能力发现、命令、JSON 输出及状态观察复用同一运行时，App 可显示外部发起执行 |
| 五 | 集成验证与交付 | 隔离运行、模拟执行、契约/类型/构建、文档及 Git 状态检查通过 |

每个可独立发布阶段判断版本，先同步 `VERSION` 与 `CHANGELOG.md`，完成验证后创建中文提交。测试源码与夹具只放在仓库外或已忽略的 `.codex-run/` 中。

## 执行记录

- 已建立 Git 标签和工作分支。后续章节记录实际变更、验证结果与回溯节点。

### 第一阶段：1.0.12

- 应用根组件移出启动校验、窗口状态和模拟订阅；节点分组与工作站展示由输入派生。
- 删除未使用的设备尺寸 props、面板状态、419 行旧展开器及指向缺失文件的脚本。
- 用户切换按请求代次接收配置，初始化的慢列表响应不能覆盖用户后续选择。
- 各构建发布入口加入版本前置检查，修复版本同步无法重复运行的问题。
- 验证：`pnpm type-check`、`pnpm lint`、`pnpm build`；忽略目录中的用户选择回归测试。构建尚有 Sass 弃用和包体积提示，样式阶段继续处理。

### 第二阶段：2.0.0

- 第一阶段提交：`7e48c2e`。
- 结构定义集中到 `database_schema.py`，迁移版本独立为 1。首次迁移事务化并在补列后建索引；保留原始实验表和数据。
- 用户列表返回真实档案；设置默认值与类型来自共享模型，设置/执行/通知共用读写服务。
- 报告升级到 3.0，由 `report_service.py` 处理 SQL 与旧记录命名；前端直接使用生成契约。操作者归属每次 execution，避免复用工作流导致归属错误。
- 删除两份没有消费者的旧设置表单。
- 因用户列表和报告接口包含不兼容清理，应用采用 MAJOR 升级 1.0.12 → 2.0.0。旧应用消费新接口需同步更新；数据库记录不做破坏性转换。
- 验证：隔离数据断言覆盖迁移、回滚、真实用户、设置合并、历史报告、文件去重；由 Python 输出真实报告夹具验证前端消费。

- 共享类型包改为 ESM，解决生成默认值常量在真实 Vite 打包中无法导入的问题；API 协议独立升级到 4.0.0。
- `pnpm build` 通过；隔离 HTTP 检查确认健康信息、schema 版本、真实用户档案与规范设置响应。命名与迁移规则见 `doc/data-contracts.md`。

### 第三阶段：2.1.0

- 第二阶段提交：`47e4ed6`。
- 展开步骤窗口重做为执行列表与详情；每页 100 项、搜索、结构收起、编号跳转、键盘操作和完整参数。
- 独立请求钩子、步骤详情和头像裁剪组件；裁剪支持指针/键盘并修复原先预览与导出缩放不一致。
- 194 个核心令牌集中管理，88 处同值颜色改为引用；移除两套图标样式桥接、旧 Sass 导入与约 1,200 行展开窗口专用样式。
- 增加 `pnpm design:check`；仅最顶层浮层响应 Escape，模态窗口管理焦点。
- 验证：6 项前端回归、类型/lint/构建，1280×720 和 900×700 的浏览器检查。Sass 弃用警告已消除；包体积提示留待集成阶段处理。
- 版本类型 MINOR：新增用户可见的浏览与定位能力，2.0.0 → 2.1.0。

### 第四阶段：2.2.0

- 第三阶段提交：`98dd0ac`。
- 新增安装入口 `uv run zahnerflow`，JSON stdout、JSON 错误 stderr、稳定退出码与 NDJSON 观察。
- 能力发现从真实节点/状态机/设备能力表生成；执行请求共享验证，CLI 共用现有 REST，不加载数据库或驱动。
- 后端进程身份和快照序号解决外部执行切换/重连，App 显示 CLI/Agent 来源并恢复终态画布。
- 修正完整参数查看读取 `config`；无效计划不先创建归档。生命周期和 room 调用同步修正。
- 验证：隔离 CLI 生命周期、陈旧 ID 拒绝、模拟设备连接、报告来源；浏览器实际显示 Agent 暂停实验；7 项前端回归与类型/lint/构建。
- 版本类型 MINOR：新增命令行和 Agent 接入，2.1.0 → 2.2.0；API 独立为 4.1.0，SQLite 迁移版本保持 1。

### 第五阶段：2.2.1

- 第四阶段提交：`7962539`。
- 报告拆出预览与共用状态展示，修复零耗时/成功标签导出、过期列表响应；报告及 PDF 库按需加载。
- 统一运行中的画布/参数编辑保护，明确后端快照恢复入口；删除无消费者的 50 份撤销历史和 8 项前端依赖、243 行未引用样式。
- 非法循环配对/次数在计划阶段拒绝。锁定 pnpm 依赖树，uv 根版本同步进入统一脚本。
- 最终前端主入口 438.14 kB（gzip 136.00 kB）；图表和 React 等独立分包，不能把主入口下降当作全部资源减少。
- 验证：9 项前端回归、类型/lint、前端/桌面构建；隔离数据库断言、CLI 生命周期和完整模拟矩阵；Windows 后端构建与二进制 HTTP/CLI 冒烟；生产页面地图/报告/展开窗口及 PDF 导出模块加载。
- 版本类型 PATCH：已有行为修复和加载/维护优化，2.2.0 → 2.2.1；API 4.1.0、报告 3.0、迁移版本 1 保持不变。
- 最终任务对应关系、回溯节点与未验证范围见 `optimization-results-2026-09-08.md`。
