# ZahnerFlow 项目代理入口

本文件保留为兼容入口，避免历史工具链或其他代理继续引用旧文件名时失效。

项目级代理规则的权威入口已经收敛到：

- `AGENTS.md`

执行任务时的最低要求：

1. 先读 `AGENTS.md`
2. 再按 `AGENTS.md` 要求读取 `.memory/rules.md` 与 `.memory/design.md`
3. 如果任务影响设计，再同步更新 `.memory/design.md` 与 `.memory/changelog.md`

如果本文件与 `AGENTS.md`、`.memory/design.md` 或真实代码冲突，以它们为准。
