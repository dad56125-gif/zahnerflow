{
  "name": "zahnerflow-desktop",
  "version": "2.3.1",
  "description": "ZAHNERFLOW Electron desktop shell",
  "author": "ZAHNERFLOW Team",
  "private": true,
  "main": "dist/main.js",
  "type": "module",
  "scripts": {
    "dev": "tsc -p tsconfig.json && electron dist/main.js",
    "build": "node ../../scripts/check-version.mjs && tsc -p tsconfig.json",
    "pack": "node ../../scripts/check-version.mjs && pnpm --filter @zahnerflow/types build && pnpm --filter zahnerflow-flowgram build && pnpm --filter zahnerflow-desktop build && pnpm -w backend:dist && electron-builder --dir",
    "dist": "node ../../scripts/check-version.mjs && pnpm --filter @zahnerflow/types build && pnpm --filter zahnerflow-flowgram build && pnpm --filter zahnerflow-desktop build && pnpm -w backend:dist && electron-builder",
    "dist:mac": "node ../../scripts/check-version.mjs && pnpm --filter @zahnerflow/types build && pnpm --filter zahnerflow-flowgram build && pnpm --filter zahnerflow-desktop build && pnpm -w backend:dist && electron-builder --mac",
    "dist:win": "node ../../scripts/check-version.mjs && pnpm --filter @zahnerflow/types build && pnpm --filter zahnerflow-flowgram build && pnpm --filter zahnerflow-desktop build && pnpm -w backend:dist && electron-builder --win --x64",
    "type-check": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^24.5.2",
    "electron": "^38.0.0",
    "electron-builder": "24.13.3",
    "typescript": "^5.9.2"
  },
  "build": {
    "appId": "com.zahnerflow.desktop",
    "productName": "ZAHNERFLOW",
    "files": [
      "dist/**/*"
    ],
    "extraResources": [
      {
        "from": "../frontend/dist",
        "to": "frontend/dist",
        "filter": [
          "**/*"
        ]
      },
      {
        "from": "../python_backend/dist",
        "to": "python_backend",
        "filter": [
          "**/*"
        ]
      }
    ],
    "directories": {
      "output": "release"
    },
    "mac": {
      "target": "dmg"
    },
    "win": {
      "target": "nsis"
    },
    "linux": {
      "target": "AppImage"
    }
  }
}
