[project]
name = "zahnerflow-runtime"
version = "1.0.0"
requires-python = ">=3.11"
dependencies = [
  "fastapi",
  "python-socketio",
  "uvicorn",
  "pyserial",
  "pydantic",
  "zahner-analysis>=1.1.5",
]

[dependency-groups]
dev = [
  "pyinstaller",
  "pytest",
]
